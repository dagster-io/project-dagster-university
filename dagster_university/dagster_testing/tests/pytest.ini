[pytest]
markers =
    integration: these tests require Docker (deselect with '-m "not integration"')
    smoke: smoke tests that run transformations on empty/synthetic data

filterwarnings =
    ignore::DeprecationWarning